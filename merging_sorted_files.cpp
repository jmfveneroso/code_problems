// =====================================================
// Book: Elements of programming interviews
// Problem 10.1: Merging sorted files
// =====================================================

#include <iostream>
#include <fstream>
#include <priority_queue>

// K-way sort just like the one implemented for inverted 
// index sorting. Uses a heap with at least the size of 
// the number of files.

using namespace std;

int main() {
  cout << "Done." << endl;
  return 0;
}
